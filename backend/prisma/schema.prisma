generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Notifier {
  id     String @id @default(uuid())
  chatId Int
  rule   String
}
